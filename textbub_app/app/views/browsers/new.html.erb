<div class="search form">
  <h5>Search By</h5>
  <%= form_tag("./search", method: "get") do %>
    <%= label_tag(:name, "Search for:") %>
    <%= text_field_tag(:name) %>
    <%= submit_tag("Search") %>
  <% end %>
</div>
